# Rung 26 anchor — freeze-out: an anchored recombination clock (numbers before code)

> **Verified anchor data** for rung 26 (a temperature/pressure-dependent chemical time `τ_chem`
> resolving WHERE nozzle recombination quenches). The feasibility **probe** was generated by
> `M:\claud_projects\temp\rung26-anchor\da_profile2.py` (outside git — project temp policy), which
> reuses the shipped rung-6/25 machinery (`_equilibrium_composition`, `build_turbojet`) read-only. The
> **shipped-integrator** numbers are from `Gas.freeze_out_nozzle` itself (the real, self-consistent
> flow). Design point: the standard real-loss cycle (`π_d=0.97, η_c=0.88, η_b=0.99, π_b=0.96, η_t=0.90,
> η_m=0.99, π_n=0.98`, `M0=0.85`, `π_c=10`), swept over `Tt4`.

## The two claims rung 25's own spec made about this seam — both check out FALSE

`docs/rung25-spec.md` tagged the freeze-out seam as needing "a `T`-dependent `τ_chem(T)` (Arrhenius)
… but reintroduces an **unanchored Arrhenius constant** (the exact trap `docs/mixing-scale-negative.md`
recorded)." Two claims, both refuted by the build:

1. **The rate IS anchored** — same mechanism the project already cites.
2. **The mechanism is NOT Arrhenius** — freeze-out is **density-driven, against an opposing `T`
   effect.**

## 1. The rates are anchored — GRI-Mech 3.0, verbatim

`gas.py:94` already cites GRI-Mech 3.0 for the five dissociation species' NASA polynomials. The
recombination sinks come from the **same** mechanism file ⇒ **zero new unanchored constants**,
categorically unlike `mixing-scale-negative.md`'s penetration exponent `p` (no anchor at all).

| Reaction | A | n | Ea (cal/mol) |
|---|---|---|---|
| `H+OH+M ⇌ H2O+M` | 2.200E+22 | −2.000 | **0.00** |
| `2H+M ⇌ H2+M`    | 1.000E+18 | −1.000 | **0.00** |
| `2O+M ⇌ O2+M`    | 1.200E+17 | −1.000 | **0.00** |
| `O+H+M ⇌ OH+M`   | 5.000E+17 | −1.000 | **0.00** |

CHEMKIN units: A in cm⁶/mol²/s (three-body), Ea in cal/mol. **Every one has `Ea = 0.00` and `n < 0`.**
`H+OH+M` is taken as the representative clock (largest `A`); the freeze *existence* and *motion* are
insensitive to the pick (all four scale identically in `T,p`). The shipped code uses `_K_HOHM_A =
2.200e22`, `_N_HOHM = -2.0`.

## 2. The mechanism is NOT Arrhenius — freeze-out is DENSITY-driven

`Ea = 0` ⇒ `k(T) = A·T^n` with `n = −1…−2` ⇒ the rate constant **accelerates as the gas cools** — it
*fights* freezing. What freezes the flow is the **density collapse**:

```
  τ_chem = 1 / ( k(T)·[OH]·[M] ) ∝ 1 / ( A·T^n · x_OH · c_tot² ),   c_tot² ∝ (p/T)²
```

`[M] ∝ p/T` craters through the expansion while the radical partner `x_OH` is itself consumed. So
`Da_local = τ_res/τ_chem` collapses on the **pressure** dependence, **against** an opposing `T`
dependence. Rung 25's framing of its own seam is wrong on the mechanism, not just the anchor.

## 3. Feasibility gate (probe) — does a TURBOJET (~8×, not a rocket's 100–1000×) freeze?

`da_profile2.py`, **bracketed** (v1's local-equilibrium evaluation was self-referential — in a cooling
flow eq-`x_OH` collapses, understating a freezing flow's radical pool). Correct bracket: **frozen**
comp (`x_OH` pinned at station 4) = radical-RICH = `Da` UPPER bound; **equil** comp = radical-POOR =
lower bound.

| `Tt4` | expansion | `Da` frozen bd.: entry → exit | crossing (frozen bd.) |
|---|---|---|---|
| 1500 | 6.29× | 0.31 → 0.043 | **never > 1 — frozen from entry** |
| 1800 | 7.40× | 1.45 → 0.17 | s ≈ 0.2 |
| 2200 | 8.55× | 4.50 → 0.45 | s ≈ 0.7 |

The crossing **exists** and is robust hot (both bounds cross at `Tt4 ≥ 1800`); at `Tt4=1500` **neither**
bound ever exceeds 1 ⇒ the lean nozzle is **frozen from entry** (an independent derivation of the
production frozen nozzle from anchored rates). **These are the PROBE's crossings, an UPPER bound.**

## 3′. The shipped integrator — the freeze point MOVES with `Tt4` (THE RUNG, verified)

`Gas.freeze_out_nozzle` marches `x_OH` **self-consistently** (default `FreezeOut(L=0.5, nstep=400)`).
As radicals recombine, `x_OH` drops ⇒ `τ_chem` rises ⇒ `Da_local` falls ⇒ the relaxation
**self-quenches**, so the real freeze point sits **upstream** of the probe's `s`:

```
   Tt4   expansion   Da_entry    Da_exit   s_freeze   CO_entry    CO_exit   frozen-from-entry
  1500     6.29×    3.098e-01   4.052e-02    0.000    5.123e-06  4.506e-06        True
  1650     6.88×    7.352e-01   7.298e-02    0.000    4.239e-05  3.238e-05        True
  1800     7.40×    1.446e+00   1.069e-01    0.118    2.523e-04  1.592e-04        False
  2000     8.01×    2.846e+00   1.442e-01    0.288    1.892e-03  8.996e-04        False
  2200     8.55×    4.501e+00   1.686e-01    0.378    1.097e-02  4.145e-03        False
```

- **`s_freeze` is strictly monotone in `Tt4`** (0.000 → 0.000 → 0.118 → 0.288 → 0.378): two lean cases
  frozen-from-entry, then the crossing **walks downstream** as it heats. This is the certified rung —
  **the physics a constant `Da` structurally cannot express.**
- **The probe over-predicted the location, as warned.** At `Tt4=2200` the probe put the crossing at
  `s ≈ 0.7`; the self-quenching integrator freezes at `s = 0.378` — earlier, exactly the direction the
  ⚠ caveat predicted. The **motion survived** the compression (the open risk that `Tt4=1800` would
  collapse toward the frozen-from-entry case did **not** materialize).
- **Frozen-in exit CO recombines DOWN** (`CO_exit < CO_entry`), and the burn-down is deeper hot
  (2200: 10.97e-3 → 4.15e-3, a 62% burn; 1500: 5.12e-6 → 4.51e-6, a 12% burn) — the load-bearing
  composition-space observable, what a downstream calc would read.

## 3″. V9 ordering (sub-percent — NOT the finding)

```
   Tt4      V9_frozen   V9_freeze   V9_irrev_fast   bracket-filled
  1500      1039.891    1039.897     1039.951           9.97%     (dormant lean)
  2200      1406.539    1409.574     1412.576          50.27%     (earns its keep hot)
```

`V9_frozen ≤ V9_freeze ≤ V9_irrev_fast` holds, but the whole `[F, I]` bracket is sub-percent (rung-25
result), so `V9_freeze` is a tiny wiggle inside it — the ordering is certified, the magnitude is not.

## 4. The kill test — density drives the freeze DESPITE an opposing `T` effect

On the **standalone clock** with `x_OH` pinned at the frozen entry (the clean isolation of `k(T)`
against `c_tot²`), `Tt4=2200` (`T` 1992→1214 K, `p` 427→50 kPa):

| factor | over the expansion | effect on `τ_chem` |
|---|---|---|
| `k(T)=A·T^n`, n=−2 | ×2.69 (accelerates) | ÷2.69 — **OPPOSES** freezing |
| `c_tot²` | ×27.1 | ×27.1 — **DRIVES** freezing |
| NET | ×10.1 | `Da_local` 4.50 → 0.45 |

- **Kill T** (`k` pinned ⇒ density alone): `Da_local → 0.166` — **STILL FREEZES.** Density did it.
- **Kill p** (`[M]` pinned ⇒ `T` alone): `Da_local → 12.1` — **NO FREEZE; it RISES 4.50 → 12.1.**

Arrhenius intuition predicts `Da` *falls* on cooling; the anchored rate makes it *rise*. Non-circular,
and the **opposite sign** — what refutes the "unanchored-Arrhenius-trap" framing on the mechanism too.

## 5. The reduce — constant `Da_local` ⇒ rung-25 `_finite_rate_expand` BIT-FOR-BIT

`_freeze_out_expand` is rung-25's loop duplicated verbatim with the scalar `Da` promoted to a per-step
`Da_local = da_local_fn(comp,T,p)`. Driven with `da_local_fn = lambda: Da` (the literal constant at the
`Da_local` level, NOT via `τ_res/τ_chem` division — a different float path), it reproduces
`_finite_rate_expand(Da)` **to the ULP** at every `Da ∈ {0.5, 2, 10}` × `nstep ∈ {100, 400}` (`T`, `V`,
`dS`, and every species float-equal). An **identity** reduce (unlike rung-25's own convergent
integrator reduce) — and the drift tripwire for the duplicated loop.

## 6. NOT claimed (the rung-16/23/24 precedent)

- **The freeze LOCATION** (`s_freeze` to any precision) and the **frozen-in composition** — the
  probe/integrator bracket is wide; location rides on `τ_res` (`L≈0.5 m`), the representative-reaction
  pick, and the frozen↔equil composition bracket.
- **The freeze-out `V9` magnitude** — rides on the same unanchored `L`; certified only to sit inside
  `[V9_F, V9_I]`.
- **The honest trade:** rung 25's `Da` was a pure cartoon; rung 26 splits it into `Da_local =
  τ_res/τ_chem(T,p)` — chemistry **anchored**, one **geometric** knob `τ_res` left. A real reduction in
  unanchored content, not zero. Freeze *existence* and *motion* survive any O(1) `L`; the *location*
  does not.

## Sources

- GRI-Mech 3.0 mechanism file (`grimech30.dat`), same source `gas.py:94` cites for the dissociation
  species; GRI-Mech 3.0 home text.
- NASA NTRS 19650044133 — Bray sudden-freezing criterion evaluation (the classical nozzle freeze-out
  criterion; termolecular recombination as "the principal chemical contribution to nozzle
  performance").
