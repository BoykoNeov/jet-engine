---
name: rungs72-77-march-audit
description: "The 72-77 march audit — the arrest is a CELL not a rig, rung 74 already owned it, and a liveness counter on a frozen plant reports FULL activity"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1a9f7c95-42c1-49bc-b17b-41e3fd7cf11d
  modified: 2026-08-10T13:41:11.696Z
---

Closed the seam `docs/rung79-gap-margin.md` § 6 left open ("rungs 72–77 share the rig — a
reason to look, NOT a result"). Result: **CONFIRMATION for 72–77, CORRECTION for 78/79.**
Shipped as `docs/rungs72-77-march-audit.md` + five gates (72/73/75/76/77), 2026-08-10.

**The three transferable lessons:**

1. **A seam's section number does not survive the rungs it points at.** "§ 5" is 78/79's
   numbering; in 72/73 it is THE LEDGER and in 74–77 WHAT THIS DOES TO THE RUNGS BEFORE IT.
   Auditing "§ 5" literally would have read six sections, five with no trajectory in them, and
   reported nothing while looking thorough. **Re-target a seam onto the code object (here: the
   march call site) before believing its address.**

2. **TWO vacuity modes, and the instrument for one scores the other GREEN** (the advisor's
   blocking catch, pre-registered before the sweep): (A) static plant — states frozen; (B)
   dormant leg — plant moves, the section's own loop never acts. **And they are not
   independent: on a frozen plant every liveness counter reports the INITIAL CONDITION at FULL
   count** — the two arrested rows carry `valve = 341/341`, the audit's highest. Mode B's
   instrument fails hardest exactly where mode A holds, so read spreads first, always.

3. **The finding was already in the repo.** Rung 74 § 2.2 "THE ARREST — disclosed, not tuned
   away" found, mechanised and gated the standstill, and sited its own arms at 0.76/0.70 to
   avoid it. Rungs 75/76/77/78/79 and the gap doc contain the string **zero times**; 78/79
   marched inside it and the gap doc rediscovered it from scratch. **Before writing a
   correction, grep the earlier rungs for the phenomenon's own name.** See
   [[rung79-gap-margin]], [[rung74-demand-coordinate]].

**The verdict:** the arrest is the CELL `(demand, φ_lim = 0.80)`, not the shared rig — 72/73/77
march in `clip` (77 at the arrested rows' OWN wall, 0.80, and it is the liveliest march in the
family), 75/76 in `demand` at 0.76. **Take a rung's settings from its READERS, never from its
test file's constants**: two of the six walls/cells were wrong on the first pass — rung 77's
`leg_slopes` derives `sm` from `PHI_JAC`, not the `PHI_BOTH` its reduce arms use, and rung 75's
`windup_bill` marches cells the obvious `(demand, sched, none)` guess is not among. New finding of its own: at `(demand, 0.76)` the
valve and stator are **inert 0/341**, so rungs 75/76's trajectories are a TWO-loop plant their
specs describe as four. Voids nothing (their claims are fuel-side); never stated before.
