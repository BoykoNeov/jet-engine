---
name: rung60-matched-floor
description: "SHIPPED rung 60 = the matched phi floor; a floor PINS the coordinate it watches so the composite is a tautology in ANY currency; the advisor's blocker BECAME the headline"
metadata: 
  node_type: memory
  type: project
  originSessionId: 146dc659-0e79-4477-b5b4-5ea05e75da43
  modified: 2026-07-28T19:37:59.725Z
---

SHIPPED rung 60 (2026-07-28) — **the MATCHED φ FLOOR**, closing rung 59's own seam.

**Headline: a limiter that FLOORS a variable PINS it**, so a floor leg's four-cell second
difference is a difference of SET POINTS, not dynamics, and takes a value fixed by the offset
between the leg's coordinate and the currency — exactly `v` for a φ floor, exactly `0` for an
incidence floor, both measured to ≤2.7e−15. Re-referencing MOVES the tautology; it does not
remove it. **A leg that SETS a minimum cannot compose, in ANY coordinate.** Only that direction is proved; the converse (relocating ⇒ composes) rests on rung 48's schedule alone and is what the next seam tests.

What made this rung work, and is worth reusing:

- **The advisor's blocker became the headline.** It flagged "your leg targets `M_i`, so on the
  cells where it binds `min M_i` is the set point and the second difference is structurally
  zero." I had been about to build a rung claiming matching *restores* composability. Probing
  the blocker in all three binding regimes (both-pinned / armed-clears / grazing) showed every
  one is tautological — which is a stronger rung than the one I set out to write. See
  [[advisor-blockers-become-findings]].
- **The advisor also blocked an arithmetic estimate built from a PUBLISHED table.** I computed
  a band gap off rung 58's spec and silently mixed two machines (its schedule's credit with its
  constant setting's band endpoints). Measuring directly kept the sign on one branch and
  INVERTED it on the other — the branch that mattered. Lesson: never do cross-rung arithmetic
  on published numbers without re-measuring on the machine you will actually run.
- **The advisor's one wrong call, surfaced rather than followed:** it said keep no load-bearing
  claim on the schedule branch (state-fed floor = new plant). The measurement made that moot in
  the opposite direction — the CONSTANT ladder is admissible out to `v = 0.190`, so the whole
  body runs on a scalar floor with **no new plant at all**. It withdrew on being shown the data.
- **A wrong gate I wrote and caught:** I asserted "φ fails on every row" when my own probe table
  showed `v = 0.05` is φ-admissible. Both coordinates have a threshold; re-referencing moves it
  (~2× in setting), it does not abolish it. Gate the IMPLICATION, not the absolute.

The surviving positive result: the admissibility criterion **`credit < excursion`** — an exact
algebraic identity whose two inputs answer to different things, so **composability has a clock
and the clock is entirely the RAMP's** (credit `r`-invariant to 0.97 %, excursion swings 4.21×).
That is rung 57's no-clock law doing work on an axis rung 57 never looked at.

Cross-rung: CONFIRMS rung 58's verdict but CORRECTS its diagnosis (disjointness is a symptom,
pinning is the disease — and rung 58's own filed-as-curiosity by-product WAS the disease) and
INVERTS its ranking (the constant setting, whose interaction was an order of magnitude smaller,
is the leg that cannot be composed). Related: [[rung58-composite-minselect]],
[[rung59-matched-schedule]], [[rung57-stator-schedule-transient]].

CLAUDE.md was at 29,978 / 30,000 bytes — adding the row required compressing four over-long
seam entries first. Do NOT raise the budget; the guard's own instruction is to move detail into
the spec.
